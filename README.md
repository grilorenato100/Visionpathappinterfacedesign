
  # VisionPath app interface design

  This is a code bundle for VisionPath app interface design. The original project is available at https://www.figma.com/design/yXGt0dCP2NxLCUGWrfHNas/VisionPath-app-interface-design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  